/* var nome = "Gabriel Cesar";
console.log("Ola, meu nome é "+nome);


let nomes = ["Gabriel", "Mislene", "Minnie", "Julia"];

console.log(nomes);

for ( let i = 0; i <nomes.length; i++) {
    let nomeCapturado = nomes[i];
}

console.log(nomes) */

let soma = [1,2,3,4];

let calcular = 0;

for (let i = 0; i < soma.length; i++) {
    calcular = calcular + soma[i]; console.log(calcular)
}

